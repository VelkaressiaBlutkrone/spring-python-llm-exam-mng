# MySQL (init 실행 후 root@'%' 사용, llm_user도 생성됨)
MYSQL_USERNAME=root
MYSQL_PASSWORD=rootpassword

# MySQL root (init-mysql.sql 실행 시 사용, app-db 컨테이너 생성 시 설정한 비밀번호와 동일해야 함)
MYSQL_ROOT_PASSWORD=rootpassword

# Python LLM 서버
LLM_SERVICE_URL=http://localhost:8000

# Spring Boot 서버 포트
SERVER_PORT=8080
